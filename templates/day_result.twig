<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>{{month}}月{{day}}日の収支</title>
</head>
<body>
    <h1>{{month}}月{{day}}日の収支</h1>
	<a href="">編集する</a>
    <h3>{{ (sums.inc - sums.spe) | number_format }}円</h3>
    <div>
        <h2>内訳</h2>
		<hr>
		{% for con in contents %}
			<h3>{{ con.subject }}<spacer>
			{% if con.income != NULL  %}
				{{ con.income | number_format }} 円
			{% elseif con.spending != NULL %}
				-{{ con.spending | number_format }} 円
			{% endif %}	
			</h3>
			<p>
			{% for tag in con.tags %}
				#{{tag}} 
			{% endfor %}
			</p>
		<hr>
		{% endfor %}
    </div>
    <div>
        <h2>週間推移</h2>
        <p>グラフ挿入予定地</p>
        <br>
    </div>
    <div>
        <a href="regist.php">記帳する</a>
        <a href="home.php">Home</a>
    </div>
</body>
</html>